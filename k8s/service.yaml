apiVersion: v1
kind: Service
metadata:
  name: notification-api-service
  labels:
    app: notification-api
spec:
  selector:
    app: notification-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
